#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies 2 integers
 * @a: takes integer
 * @b: takes integer
 * Return: Always 0
 */
int mul(int a, int b)
{
	int num;

	num = a * b;
	return (num);
}
